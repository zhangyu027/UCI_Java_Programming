package StudentFinalProject;

public class Student extends Person {

    public Student(int id, String name) {
        super(id, name);
    }

    // TODO:
    // Add one Student-specific field or behavior.
}