package inclass.m5;

/*
* IN-CLASS STUDENT HINTS - RECURSION
* ----------------------------------
* Write the base case first.
* Then make the recursive call solve a smaller input.
* Trace a small example by hand before testing a larger one.
*/
/** Module 5 recursion practice: sum the digits of an integer. */
public class SumOfDigits {
    static int sum(int number) {
        // TODO: Implement recursively.
        /*
        * HINT:
        * Last digit: number % 10
        * Remaining digits: number / 10
        * Base case can be when number has only one digit.
        */
        return 0;
    }
    public static void main(String[] args) {
        System.out.println("TODO: test recursive sum of digits.");
    }
}
