package inclass.m5;

/*
* IN-CLASS STUDENT HINTS - RECURSION
* ----------------------------------
* Write the base case first.
* Then make the recursive call solve a smaller input.
* Trace a small example by hand before testing a larger one.
*/
/** Module 5 recursion practice: Tower of Hanoi. */
public class TowerOfHanoi {
    static void move(int disks, char from, char to, char auxiliary) {
        // TODO: Implement recursive moves.
        /*
        * HINT:
        * 1. Move n-1 disks from FROM to AUXILIARY.
        * 2. Move disk n from FROM to TO.
        * 3. Move n-1 disks from AUXILIARY to TO.
        * Base case: one disk.
        */
    }
    public static void main(String[] args) {
        System.out.println("TODO: call move(3, 'A', 'C', 'B').");
    }
}
