CREATE DATABASE IF NOT EXISTS University; USE University; CREATE TABLE IF NOT EXISTS Students(studentID INT PRIMARY KEY,firstName VARCHAR(50),lastName VARCHAR(50)); CREATE TABLE IF NOT EXISTS Courses(courseID INT PRIMARY KEY,studentID INT,courseTitle VARCHAR(100),CONSTRAINT fk_student FOREIGN KEY(studentID) REFERENCES Students(studentID));